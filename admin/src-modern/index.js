/**
 * Modern host shell entry point (@wordpress/scripts build).
 *
 * Placeholder for the modern Admin host shell. This file exists so the wp-scripts pipeline emits
 * `admin/build/index.js` + `admin/build/index.asset.php`, establishing the build + enqueue pattern
 * (see includes/assets.php → pixassist_enqueue_built_script). The actual host shell UI lands in #43.
 *
 * Intentionally a no-op for now — no feature change.
 */
